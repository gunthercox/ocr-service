# docker-compose file for development and testing
services:

  app:
    build:
      context: .
      target: all
      args:
        INSTALL_DEV: ${INSTALL_DEV:-true}
    ports:
      - "6000:5000"
    volumes:
      - .:/code
